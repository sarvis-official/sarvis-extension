node esbuild.js

vsce package --allow-missing-repository

code --install-extension sarvis-1.0.5.vsix --force

git status

git add .


git add src/extension.ts
git add README.md

git add file1 file2 file3

git commit -m "Fix authentication issue"

git push origin development


git push origin main

git add -p
y = stage this change
n = skip
q = quit
s = split smaller chunks
e = manually edit

